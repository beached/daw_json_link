name: Windows

on:
  push:
    branches: [ release, develop ]
  pull_request:
    push:
      branches-ignore: develop
    pull_request:
      branches-ignore: develop

jobs:
  MSVC:
    defaults:
      run:
        shell: cmd
    strategy:
      fail-fast: false
      matrix:
        cpp_version: [ 17, 20 ]
        build_type: [ Debug, Release ]
        arch: [ x64, Win32 ]
        os: [ windows-2019, windows-2022 ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
      - name: Create Build
        run: |
          md build_${{ matrix.cpp_version }}_${{ matrix.arch }}_${{ matrix.build_type }}
      - name: CMake Config
        run: |
          cmake -A ${{ matrix.arch }} -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} -DCMAKE_CXX_STANDARD=${{ matrix.cpp_version }} -DDAW_NUM_RUNS=1 -DDAW_ENABLE_TESTING=On -DDAW_WERROR=ON build_${{ matrix.cpp_version }}_${{ matrix.arch }}_${{ matrix.build_type }}/
      - name: Build
        run: |
          cd build_${{ matrix.cpp_version }}_${{ matrix.arch }}_${{ matrix.build_type }}
          cmake --target full --config ${{ matrix.build_type }} -j 3
      - name: Test
        run: |
          cd build_${{ matrix.cpp_version }}_${{ matrix.arch }}_${{ matrix.build_type }}
          ctest -C ${{ matrix.build_type }} -j2 -VV --timeout 1200

#  Win_clang-cl:
#    strategy:
#      matrix:
#        os: [ 2019 ]
#        cpp_version: [ 17, 20 ]
#        build_type: [ Debug, Release ]
#    runs-on: windows-${{ matrix.os }}
#    steps:
#      - uses: actions/checkout@v1
#      - name: Build
#        run: .\ci_scripts\ci_windows_build_clangcl.cmd { matrix.os }} ${{ matrix.build_type }} ${{ matrix.cpp_version }}
#      - name: Test
#        run: .\ci_scripts\ci_windows_test.cmd ${{ matrix.os }} ${{ matrix.build_type }}
#
